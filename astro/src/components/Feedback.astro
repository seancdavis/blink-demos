---
export type Props = {
  message?: string
  classname?: string
}

const { message, classname } = Astro.props
---

<div class={`feedback container-xs ${classname}`}>
  <small>{message}</small>
</div>
